# URL of your n8n instance (used by Vite dev server proxy — NOT exposed to browser)
N8N_URL=http://localhost:5678

# API base URL for the frontend (leave blank to use Vite dev proxy in development)
# In production, set this to your n8n public URL, e.g. https://your-n8n-instance.com
VITE_API_URL=
